apiVersion: v1
kind: Service
metadata:
  name: guestbook-service
  labels:
    app: guestbook
spec:
  selector:
    app: guestbook
  ports:
    - protocol: TCP
      port: 3000
      targetPort: http
      name: http
